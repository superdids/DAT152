"use strict";

/**
 * Globalt JSON-objekt som holder på en egenskap som representerer
 * URL-en til en Service lokalisert på localhost:8080 domenet.
 */
var config = {
		servicesPath: "http://localhost:8080/Mservices/data"
};