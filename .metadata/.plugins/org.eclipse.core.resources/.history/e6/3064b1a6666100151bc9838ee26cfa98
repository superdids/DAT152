"use strict";

/**
 * @author Didrik Emil Aubert
 * Constructorfunction to manage single member element.
 */

function MemberManager(table,member) {
	this._table = table;
	this._member = member;
}

MemberManager.prototype.updateMember(id) {
	var row = document.getElementById("m"+id);
	row.cells[0] = member.firstname;
	row.cells[1] = member.firstname;
	row.cells[2] = member.firstname;
	row.cells[3] = member.firstname;
}