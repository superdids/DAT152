"use strict";

var symbols = [];
window.onload = function() {
	var elm = document.getElementById('digital');
	elm.rows = 15;
	elm.cols = 75;
	
	for(var x = 0; x < 50; x++) {
		elm.value += "w";
	}
}

function rand(var) {
	return Math.floor(Math.random() * 50);
}


