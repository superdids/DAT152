(function() {
	var lastet = function() {
		function forsinketUtskrift(tekst) {
			window.setTimeout("window.alert(tekst)", 10000);
		}
		forsinketUtskrift("En liten utskrift");")
	}
	
	window.addEventListener('load', lastet, true);
})();