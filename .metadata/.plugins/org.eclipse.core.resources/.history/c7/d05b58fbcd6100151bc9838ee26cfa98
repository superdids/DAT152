"use strict";

function Controller(parent) {
	this._parent = parent;
	this._gui = new GUIManager(this._parent, -1);
	this._http = new HTTPOperations("/endringer", gui.updateMemberList.bind(gui));
}

Controller.prototype.start = function() {
	this._http.GetMembersByLogId(this._gui.getLogId());
	
}

