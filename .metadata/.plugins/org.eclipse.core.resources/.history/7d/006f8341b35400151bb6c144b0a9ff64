"use strict";

(function() {

	function lastet() {
		document.getElementById("newMember").addEventListener("click", newMember, false);
		
		var url = servicesPath.path + "/endringer";
		var ajax = new AJAXConnection(url);
		ajax.onsuccess = members;
		ajax.get(-1);
	
	};
	
	function members(m) {
//		var memebers = JSON.parse(m);
//		memebers = JSON.stringify(members);
		var members = {};
		members = JSON.parse(m);
		members = members.updates;
//		alert(members.updates);
//		console.log(JSON.parse(m).updates);
//		alert(members);
//		alert(members['updates']);
		
//		for(var key in members) {
//			console.log(key);
//		}
	}
	
	function newMember() {
		//TODO
		alert(3);
	}
	
	window.addEventListener("DOMContentLoaded", lastet, false);
})();